/* Circuit Simulator --- Components.java
 *
 * @author Alvyn Berg
 *
 * Comopnents class, containing definitions for all objects used in basic 
 * circuits: breadboard, voltage source, ground, conductive wires, resistors,
 * capacitors, LEDs, potentiometers, and buttons. */

// learn to work with packages.

/* Breadboard class. 
 * This first iteration will use an adjacency matrix (2D array)
 * */
public class Socket {
	// create generic types. 
	// We will connect anything to the sockets. 
}

public class Breadboard {
	 private char[][] board;

	 public Breadboard(int rows, int columns) {
		 this.board = new char[rows][columns];
	 }

}

